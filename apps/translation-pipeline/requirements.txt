# 실제로 설치해 동작을 확인한 버전이다.
# pythonpath 설정을 쓰므로 pytest 7.0 이상이 필요하다.
pytest>=7.0

# 4단계 번역 provider
google-genai>=2.18.1

# Gemini 무료 티어 한도(429) 초과 시 대체 provider
openai>=2.54.0

# 2·6단계 Deepgram 음성 인식
deepgram-sdk>=7.7.0

# 마이크 입력. sounddevice는 PortAudio를 쓰고 numpy 배열로 오디오를 다룬다.
sounddevice>=0.5.5
numpy>=2.2.6

# 회의방 오디오를 받는 에이전트. livekit은 실시간(rtc), livekit-api는 토큰 발급.
livekit>=1.1.14
livekit-api>=1.2.0
# 워커 등록과 방 배정. 프레임워크의 STT 플러그인은 쓰지 않는다.
livekit-agents>=1.6.10

# .env에서 API 키를 읽는다
python-dotenv>=1.2.2

# 이후 단계에서 추가한다. 실제로 설치한 버전을 확인한 뒤 적는다.
#   Claude로 전환할 경우: anthropic
